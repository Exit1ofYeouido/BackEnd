service-url: /api

server:
  port: 8080
  servlet:
    context-path: ${service-url}

spring:
  token:
    secret: ${SECRET}
  application:
    name: api-gateway
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: 'http://3.36.196.194:5173'
            allow-credentials: true
            allowedHeaders: '*'
            allowedMethods:
              - PUT
              - GET
              - POST
              - DELETE
              - OPTIONS
      httpclient:
        wiretap: true
      httpserver:
        wiretap: true
      routes:
        - id: basic
          uri: http://localhost:8081
          predicates:
            - Path= ${service-url}/basic/**
          filters:
            - RewritePath=${service-url}/basic/(?<path>.*),/$\{path}
            - AuthorizationHeaderFilter

        - id: mypage
          uri: http://3.36.68.97:8082
          predicates:
            - Path= ${service-url}/mypage/**
          filters:
            - RewritePath=${service-url}/mypage/(?<path>.*),/$\{path}
            - AuthorizationHeaderFilter

        - id: reward
          uri: http://43.203.224.71:8083
          predicates:
            - Path= ${service-url}/reward/**
          filters:
            - RewritePath=${service-url}/reward/(?<path>.*),/$\{path}
            - AuthorizationHeaderFilter

        - id: search
          uri: http://localhost:8084
          predicates:
            - Path= ${service-url}/search/**
          filters:
            - RewritePath=${service-url}/search/(?<path>.*),/$\{path}

        - id: auth
          uri: http://3.39.235.153:8085
          predicates:
            - Path= ${service-url}/auth/**
          filters:
            - RewritePath=${service-url}/auth/(?<path>.*),/$\{path}




springdoc:
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8

  webjars:
    prefix: ${service-url}
  swagger-ui:
    operations-sorter: alpha
    use-root-path: false
    urls:
      - url: /v3/api-docs
        name: API Gateway Service
      - url: ${service-url}/basic/v3/api-docs
        name: Basic
      - url: ${service-url}/mypage/v3/api-docs
        name: Mypage
      - url: ${service-url}/reward/v3/api-docs
        name: Reward
      - url: ${service-url}/auth/v3/api-docs
        name: Auth
      - url: ${service-url}/search/v3/api-docs
        name: Search